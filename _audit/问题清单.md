# BSAI-MiniMAX-H3-Prompt 模板体检 — 问题清单

- 总模板数：**518**
- 体检源文件 1：`web/templates_data.json`（前端，2125.7KB）
- 体检源文件 2：`templates/prompt_templates.json`（后端，2133.5KB）
- 体检时间：2026-09-09T14:50:33.930Z


## A · 图N 角色不一致（declared vs actual）—— HIGH（0 条）

> 无


## C · 缺参考图铁律头 —— HIGH（2 条）

| id | 字段 | 严重度 | 现象 | 建议修法 |
|---|---|---|---|---|
| `water_splash_wardrobe` | prompt[head] | high | prompt 引用了 <Picture 1, 2, 3>，但开头没有 CRITICAL REFERENCE LOCK / 参考图铁律 强声明 | 在 prompt 头部补 [CRITICAL REFERENCE LOCK / 参考图铁律 - HIGHEST PRIORITY] 块，并逐图明确 "<Picture N> = the ROLE" 的硬约束。 |
| `action_anchor_snap_swap` | prompt[head] | high | prompt 引用了 <Picture 1, 2, 3>，但开头没有 CRITICAL REFERENCE LOCK / 参考图铁律 强声明 | 在 prompt 头部补 [CRITICAL REFERENCE LOCK / 参考图铁律 - HIGHEST PRIORITY] 块，并逐图明确 "<Picture N> = the ROLE" 的硬约束。 |


## B · 声明可选但无回退分支 —— MEDIUM（23 条）

| id | 字段 | 严重度 | 现象 | 建议修法 |
|---|---|---|---|---|
| `sketch_product_water_awakening` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `sketch_product_light_stroke` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `sketch_product_spray_paint` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `kid_doodle_dinosaur` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `kid_doodle_cat` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `kid_doodle_house` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `lineart_girl_runway` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `lineart_girl_dance_rain` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `sketch_dragon_fantasy` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `sketch_f1_racecar` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `drawing_frame_escape` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `kid_doodle_food` | Picture 1 | medium | 图1（?）声明为"可选"，但 prompt 没有"if no <Picture 1>" / "未提供图1" / "该图<Picture 1> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 1> is not provided, ..."的回退逻辑。 |
| `combat_bullet_time_duel_multi` | Picture 3 | medium | 图3（场景）声明为"可选"，但 prompt 没有"if no <Picture 3>" / "未提供图3" / "场景<Picture 3> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 3> is not provided, ..."的回退逻辑。 |
| `mi_two_champions` | Picture 3 | medium | 图3（场景）声明为"可选"，但 prompt 没有"if no <Picture 3>" / "未提供图3" / "场景<Picture 3> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 3> is not provided, ..."的回退逻辑。 |
| `mi_one_vs_many` | Picture 3 | medium | 图3（场景）声明为"可选"，但 prompt 没有"if no <Picture 3>" / "未提供图3" / "场景<Picture 3> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 3> is not provided, ..."的回退逻辑。 |
| `mi_asset_separated` | Picture 3 | medium | 图3（场景）声明为"可选"，但 prompt 没有"if no <Picture 3>" / "未提供图3" / "场景<Picture 3> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 3> is not provided, ..."的回退逻辑。 |
| `eighteen_weapons_duel` | Picture 3 | medium | 图3（场景）声明为"可选"，但 prompt 没有"if no <Picture 3>" / "未提供图3" / "场景<Picture 3> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 3> is not provided, ..."的回退逻辑。 |
| `magic_mediator_wardrobe_multi` | Picture 3 | medium | 图3（服装）声明为"可选"，但 prompt 没有"if no <Picture 3>" / "未提供图3" / "服装<Picture 3> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 3> is not provided, ..."的回退逻辑。 |
| `water_splash_wardrobe` | Picture 3 | medium | 图3（场景）声明为"可选"，但 prompt 没有"if no <Picture 3>" / "未提供图3" / "场景<Picture 3> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 3> is not provided, ..."的回退逻辑。 |
| `beatsync_outfit_swap` | Picture 3 | medium | 图3（场景）声明为"可选"，但 prompt 没有"if no <Picture 3>" / "未提供图3" / "场景<Picture 3> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 3> is not provided, ..."的回退逻辑。 |
| `action_anchor_snap_swap` | Picture 3 | medium | 图3（场景）声明为"可选"，但 prompt 没有"if no <Picture 3>" / "未提供图3" / "场景<Picture 3> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 3> is not provided, ..."的回退逻辑。 |
| `turn_push_wardrobe` | Picture 3 | medium | 图3（场景）声明为"可选"，但 prompt 没有"if no <Picture 3>" / "未提供图3" / "场景<Picture 3> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 3> is not provided, ..."的回退逻辑。 |
| `handwipe_screen_transition` | Picture 3 | medium | 图3（场景）声明为"可选"，但 prompt 没有"if no <Picture 3>" / "未提供图3" / "场景<Picture 3> 缺图时"之类的回退说明 | 在 prompt 头部或该图赋值句后追加"if <Picture 3> is not provided, ..."的回退逻辑。 |


## A · description 声明了图N 但 prompt 未引用 —— LOW（多数为 description 写法过度）（452 条）

| id | 字段 | 严重度 | 现象 | 建议修法 |
|---|---|---|---|---|
| `lineart_to_dance` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `lineart_to_walking` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `lineart_to_anime` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `lineart_to_3d` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `portrait_to_speaking` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `fullbody_to_action` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `char_to_turnaround` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `product_360` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `product_lifestyle` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `landscape_flythrough` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `interior_walk` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `photo_to_anime` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `photo_to_watercolor` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `photo_to_oilpaint` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `anime_to_live_action` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `sketch_product_water_awakening` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `sketch_product_light_stroke` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `sketch_product_spray_paint` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `kid_doodle_dinosaur` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `kid_doodle_cat` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `kid_doodle_house` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `lineart_girl_runway` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `lineart_girl_dance_rain` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `sketch_dragon_fantasy` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `sketch_f1_racecar` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `drawing_frame_escape` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `kid_doodle_food` | Picture 2 | low | description/generation_mode 声明了"图2=场景"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `seed_to_bloom` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `vine_climbing` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mushroom_growth` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `tree_four_seasons` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `caterpillar_to_butterfly` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `tadpole_to_frog` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `hatchling_to_bird` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `puppy_growing` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `baby_to_adult` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `aging_portrait` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `life_journey` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `character_metamorphosis` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `crystal_growth` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `city_expansion` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `skyscraper_construction` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `city_day_night` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `interior_renovation` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `bridge_completion` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `exploded_view_assembly` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `printer_3d_printing` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `smartwatch_assembly` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `block_building` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `highway_light_trails` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `interchange_traffic` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `subway_station` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `port_shipping` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `sea_of_clouds` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `star_trails` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `tidal_flow` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `aurora_dance` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `rain_cyberpunk` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `epic_battle` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `suspense_noir` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `street_dance` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ballet_stage` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ai_party_bounce` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mountain_sunrise` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ocean_waves` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `season_change` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `space_walk` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `magic_forest` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `time_travel` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cafe_morning` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `subway_commute` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `day_to_night` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `season_transition` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `face_morph` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `object_transform` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_smile` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_laugh` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_cry` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_anger` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_surprise` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_fear` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_joy` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_disgust` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_maniac_laugh` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_wail` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_rage` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_contempt` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_confusion` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_shy` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_disappointment` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `expr_smug` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_hold_tears` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_smile_nothing` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_polite_anger` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_feign_calm` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_jealous_smile` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_breakdown_cared` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_words_halt` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_tired_smile` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_gaze_realization` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_awkward_smile` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_forgiven_break` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_disbelieve_joy` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `mexpr_smile_vanish` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `etrans_smile_to_cry` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `etrans_anger_to_silence` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `etrans_numb_to_warm` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `etrans_shock_to_grief` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `etrans_shame_to_anger` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `etrans_happy_tears` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `etrans_disgust_to_sneer` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `smask_polite_long` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `smask_congrats` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `smask_public_hold` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `smask_exposed_smile` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `smask_pretend_relaxed` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `bp_smile_fail` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `bp_breath_break` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `bp_delayed_tears` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `bp_hollow_laugh` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `bp_long_silence` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_sanda_vs_wingchun` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_boxing_exchange` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_close_grappling` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_kick_chain` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_street_brawl` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_sanda_vs_wingchun_multi` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_dual_blade_vs_spear` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_sword_duel` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_saber_duel` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_hidden_weapons` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_slowmo_impact` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_variable_speed` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_bullet_time_dodge` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_freeze_burst` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_bullet_time_dodge_catch` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_continuous_tracking` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_whip_pan` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_low_angle_hero` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_handheld_chaos` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_one_vs_many` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_formation_clash` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_melee_chaos` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_energy_burst` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_finishing_blow` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `combat_signature_move` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `tw_sanda_wingchun` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `tw_knockout` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `tw_slowmo_finisher` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `tw_wall_beatdown` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wd_spear_vs_saber` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wd_sword_vs_staff` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wd_baton_vs_knife` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wd_guandao_vs_twinblades` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wd_three_section_staff_vs_katana` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wd_meteor_hammer_vs_sword` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wd_horizontal_blade_vs_twin_axes` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wd_staff_vs_katana` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ma_muaythai_vs_boxing` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ma_bjj_vs_wrestling` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ma_mma_mixed` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ma_taiji_vs_sanda` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ma_bajiquan_vs_wingchun` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ma_jeet_kune_do_vs_taekwondo` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ovm_visual_brawl` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ovm_one_take_melee` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ovm_one_shot_quick_kill` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `ovm_corridor_gun_kata` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wb_ancient_charge` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wb_cavalry_charge` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wb_phalanx_vs_cavalry` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `wb_naval_battle` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_push_in` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_pull_out` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_pan_left` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_pan_right` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_tilt_up` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_tilt_down` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_truck` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_pedestal` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_whip_pan` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_complex_move` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_slow_push_emotion` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_long_take` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_slider_dolly` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_speed_ramp` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_foreground_occlusion` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_pull_away` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_tracking` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_side_follow` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_orbit` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_360_orbit` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_steadicam` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_handheld` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_lateral_shift` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_side_by_side_run` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_gimbal_tracking` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_dolly_zoom` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_zoom_in` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_zoom_out` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_rack_focus` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_infinite_zoom` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_earth_zoom` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_whip_zoom` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_low_angle` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_high_angle` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_top_down` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_dutch_angle` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_roll` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_pov` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_ots` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_probe_lens` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_overhead_reveal` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_aerial` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_drone_fly` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_crane` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_dolly_through` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_fpv` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_bullet_time` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_vertical_drop` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_spiral_ascent` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_cable_cam` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_robotic_arm` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_hero_entrance` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_dialogue_reverse` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_emotional_breakdown` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_action_fight` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_whip_transition` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_environment_reveal` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_flashback` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_suspense_thriller` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_romantic_intimate` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_chase_run` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_vehicle_driving` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_dance_performance` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_ending_finale` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_multi_standoff` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_prop_macro` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_side_peek` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_freeze_gaze` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_rapid_push` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_focus_pull_transfer` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_zoom_suffocate` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_silhouette_resolve` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_macro_loss_control` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_telephoto_detach` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_slow_pan_omen` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_empty_lingering` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_arc_approach` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_reverse_flank` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_oppressive_approach` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_yield_reveal` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_cross_traverse` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_ground_skim` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_top_spin_descend` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_sudden_stop_gaze` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_pullback_reveal` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cam_rhythm_burst` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_kodak2383` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_kodak2393` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_fuji3513` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_vision3` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_log` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_grain` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_teal` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_blockbuster` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_commercial` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_bleach` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_desat` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_muted` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_golden` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_cool` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_dark` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_dreamy` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_sepia` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_horror` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_bwhigh` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_bwsoft` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_noir` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_infrared` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_silver` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_bwfilm` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_polaroid` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_longexp` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_doubleexp` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_tilt` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_daguerreo` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `grade_vintage` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_watercolor` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_oilclassic` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_impressionist` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_inkwash` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_sketch` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_pointillism` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_gouache` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_impasto` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_ukiyoe` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_gongbi` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_woodcut` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_papercut` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_stainedglass` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_artnouveau` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_cubism` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_popart` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_anime` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_anime90` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_ghibli` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_shinkai` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_manhua` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_comic` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_childrenbook` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_conceptart` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_pixar` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_lowpoly` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_pixel` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_cyberpunk` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_vaporwave` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_flat` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_clay` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_origami` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_doubleexp` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_longexp` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_tiltshift` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_polaroid` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_infrared` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_bw` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_surreal` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_minimal` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_anime_to_real_i2v` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `st_anime_to_real_firstframe` | Picture 2 | low | description/generation_mode 声明了"图2=人物"，但 prompt 完全没引用 <Picture 2> | 要么在 prompt 里补上 <Picture 2> 的引用，要么从 description 中删除该图编号声明。 |
| `text_age_swap` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_frame_in_frame` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_foreground_obstruction` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_leading_line` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_negative_space` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_spiral_composition` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_rule_of_thirds` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_symmetry_hall` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_diagonal_rooftop` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_low_angle_monument` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_top_down_crowd` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_frame_moongate` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_foreground_bokeh` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_background_bokeh` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_tilt_shift_chip` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_macro_insect` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_minimal_solitude` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_ots_cliff` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_hand_macro_coffee` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_chandelier_rotate` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_subway_farewell` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_ots_archive` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_closeup_lighter` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_golden_hour_cliff` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_blue_hour_arcology` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_backlit_silhouette` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_candle_cabin` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_tyndall_valley` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_rembrandt_tavern` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_practical_lamp_noir` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_moonlight_dance` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_neon_rain` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_bioluminescent_forest` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_headlight_chase` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_stage_spotlight` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_dust_spotlight` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_highkey_daylight` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_intimate_window` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_push_track_subway` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_oneshot_apartment` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_slowmo_neon` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_handheld_chase` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_pan_wasteland` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_orbit_dancer` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_drone_dive_cliff` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_underwater_ballet` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_aerial_forest` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_timelapse_city` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_whip_pan` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_dolly_zoom` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_rain_tram_transition` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_dissolve_dualtime` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_pov_sprint` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_focus_shift` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_montage_fragments` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_teal_orange_neo` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_bw_noir_alley` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_cyberpunk_portrait` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_morandi_morning` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_warm_cafe` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_kodak_taxi` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_fuji_nature` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_newsreel_bw` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_sports_contrast` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_ektachrome_stage` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_teal_cyan_scifi` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_warm_gold_text` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_rain_taxi_docu` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_fog_city_wake` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_abandoned_park` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_seaside_run` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_magic_forest` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_steam_grates` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_fireworks_river` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_night_market` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_noir_detective` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_snow_walk` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_underwater_ocean` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_late_night_store` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_japan_rain_alley` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_candle_reading` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_desert_dusk` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_city_dusk_skyline` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_snow_cabin_exterior` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_film_noir_ballroom` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_verite_market` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_scifi_hologram` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_thriller_factory` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_fashion_desert` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_war_epic` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_romance_starry` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_documentary_amazon` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_mv_neon` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_oneshot_ride` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_deakins_natural` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_lubezki_longtake` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_kaminski_war` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_intimate_portrait` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_fantasy_garden` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_neo_classical_ballet` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_scifi_lab` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_retro_roadtrip` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_heist_montage` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_virtual_studio` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_high_stakes_racing` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `cp_street_rain_poem` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_crash_zoom_reveal` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_low_angle_hero` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_slow_motion_sacrifice` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_whip_pan_duel` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_technicolor_jianghu` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_changcheh_blood` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_vintage_amber` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_neo_palette` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_rainstorm_montage` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_metric_rhythm` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_constructive_leap` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_empty_shot_zen` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_toplight_dual_face` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_backlight_silhouette` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_red_blue_drama` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |
| `shaw_kinghu_bamboo` | Picture 3 | low | description/generation_mode 声明了"图3=场景"，但 prompt 完全没引用 <Picture 3> | 要么在 prompt 里补上 <Picture 3> 的引用，要么从 description 中删除该图编号声明。 |

